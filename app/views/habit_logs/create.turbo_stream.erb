<% case params[:dom_from] %>
<% when "home" %>

  <% if @habit_logs_count.to_i > 0 %>
    <%= turbo_stream.remove "habit_logs_home_empty" %>
  <% end %>

  <%= turbo_stream.prepend "habit_logs_home" do %>
    <%= render "home/partials/card_habit_log_home", habit_log: @habit_log %>
  <% end %>

<% when "index" %>
  <%= turbo_stream.prepend "habit_logs_index" do %>
    <%= render "logs/partials/habit_log_index_card", habit_log: @habit_log %>
  <% end %>
<% end %>

<%= turbo_stream.replace "flash_area" do %>
  <%= render "shared/flash" %>
<% end %>

<!-- モーダルを閉じる -->
<%= turbo_stream.update "modal", "" %>
