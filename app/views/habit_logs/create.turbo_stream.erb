<% if @habit_log_home_card %>
  <%= turbo_stream.prepend "habit_logs_home",
        partial: "home/partials/habit_log_home_card",
        locals: { habit_log: @habit_log_home_card } %>
<% end %>

<% if @habit_log_index_card %>
  <%= turbo_stream.prepend "habit_logs_index",
        partial: "habit_logs/habit_log_index_card",
        locals: { habit_log: @habit_log_index_card } %>
<% end %>

<%#= turbo_stream.replace "flash", partial: "shared/flash" %>

<!-- replaceを使うとタグごと消えてモーダルが開けなくなるためupdateを使用 -->
<%= turbo_stream.update "modal", "" %> <!-- モーダルの中身を空にする -->
